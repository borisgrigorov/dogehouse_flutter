name: dogehouse_flutter
description: Taking voice conversations to the moon !
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^0.13.0
  web_socket_channel: ^2.0.0
  provider: ^5.0.0
  flutter_signin_button: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - Assets/logo.png
